# ================================================================================
# FASHION-VTON + TRELLIS.2 PIPELINE — REQUIREMENTS
# Môi trường: Kaggle / Colab, GPU T4/P100/A100, Linux, CUDA 12.x, Python 3.10+
# ================================================================================

# ── Core ML & Deep Learning ──────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0
# Cài torch CUDA riêng trên Kaggle/Colab:
# pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu121

# ── HuggingFace Ecosystem ────────────────────────────────────────────────────
diffusers>=0.25.0
transformers>=4.38.0
accelerate>=0.27.0
huggingface_hub>=0.22.0
safetensors>=0.4.0
peft>=0.9.0

# ── Fashn-VTON (2D Virtual Try-On) ──────────────────────────────────────────
git+https://github.com/fashn-AI/fashn-vton-1.5.git

# ── TRELLIS.2 (3D Generation) ────────────────────────────────────────────────
# QUAN TRỌNG: TRELLIS.2 KHÔNG cài qua pip thông thường.
# Phải clone repo và chạy setup.sh trên Kaggle/Colab:
#
#   git clone https://github.com/microsoft/TRELLIS /kaggle/working/TRELLIS
#   cd /kaggle/working/TRELLIS && bash setup.sh
#   pip install -e .
#
# setup.sh sẽ tự động cài: spconv, flash-attn, nvdiffrast, kaolin, ...

# ── Tách nền (Background Removal) ───────────────────────────────────────────
rembg[gpu]>=2.0.55
# Model u2net_human_seg sẽ tự tải về lần đầu chạy

# ── Xử lý ảnh ───────────────────────────────────────────────────────────────
Pillow>=10.0.0
opencv-python>=4.9.0
numpy>=1.24.0,<2.0.0

# ── Xuất & Render video ──────────────────────────────────────────────────────
imageio>=2.34.0
imageio[ffmpeg]
imageio-ffmpeg>=0.4.9

# ── Xuất 3D Mesh ─────────────────────────────────────────────────────────────
trimesh[easy]>=4.3.0
pygltflib>=1.16.0
plyfile>=1.0.3

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm>=4.66.0
scipy>=1.12.0
scikit-learn>=1.4.0
einops>=0.7.0
omegaconf>=2.3.0
gradio>=4.0.0
open3d>=0.18.0
spconv-cu126>=2.3.6
git+https://github.com/EasternJournalist/utils3d.git

# ── Monitoring & Logging (tùy chọn) ─────────────────────────────────────────
# wandb  # Bỏ comment nếu cần logging
# tensorboard
